name: flutter_7
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0" # Updated to be compatible with Dart 3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8 # Updated to the latest version
  flutter_slidable: ^3.1.1 # Updated to the latest version
  intl: ^0.20.0 # Updated to the latest version
  http: ^1.2.2 # Updated to the latest version
  google_fonts: ^6.2.1 # Updated to the latest version
  animated_text_kit: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0 # Updated to the latest version

flutter:
  uses-material-design: true
